
tasks:
  build-deps:
    runner: razorci/openjdk:11-buster
    steps:
    - checkout
    - commands:
    - run: mvn clean test
    - reports/junit: 
      - target/surefire-reports/TEST-*.xml
    - codecov/upload
         file: covarage.xml
